namespace SimpleMockServer.Domain.Matching.Matchers.Body;

internal class BodyRequestMatcher : IRequestMatcher
{
    public bool IsMatch(RequestData data)
    {
        throw new NotImplementedException();
    }
}